# Table 5: style-agnostic — train on dpo_D1.json (mask-inner phrases only)
loss:
  alpha: 0.01
  gamma: 0.0
  lambda: 0.5
  beta: 0.1

data:
  preference_file: dpo_D1.json
  note: "Short masked phrases only"
